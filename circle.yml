machine:
  timezone: Europe/Berlin

  node:
    version: iojs-v2.5.0

  services:
    - rabbitmq-server

  hosts:
    local.wolkenkit.io: 127.0.0.1

dependencies:
  pre:
    - npm install -g grunt-cli

test:
  override:
    - grunt
