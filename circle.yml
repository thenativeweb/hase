machine:
  timezone: Europe/Berlin

  environment:
    RABBITMQ_URL: amqp://localhost:5673

  node:
    version: 6.2.0

  services:
    - docker

dependencies:
  pre:
    - npm install -g roboter-cli

test:
  override:
    - bot
