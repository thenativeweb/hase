machine:
  timezone: Europe/Berlin

  node:
    version: 6.2.0

  services:
    - rabbitmq-server

  hosts:
    local.wolkenkit.io: 127.0.0.1

dependencies:
  pre:
    - npm install -g roboter

test:
  override:
    - bot
